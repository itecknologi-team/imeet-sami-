# Leave unset to let api.ts derive this from window.location (works for both
# localhost and LAN-IP access, over the right protocol). Only set this if
# fronting the app with a reverse proxy that has its own fixed hostname.
# VITE_API_URL=http://localhost:4000
