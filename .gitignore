node_modules/
dist/
.env
# Every real deployment env file, however it's named. deploy/.env.production.example
# (placeholders only) is the one that is meant to be committed.
.env.*
!.env.example
!deploy/.env.production.example
*.log
.DS_Store
*.tsbuildinfo
.claude/*
!.claude/skills/
# Locally-generated mkcert dev TLS certs (contains private keys) — see
# backend/src/config/env.ts and frontend/vite.config.ts for how they're used.
certs/
